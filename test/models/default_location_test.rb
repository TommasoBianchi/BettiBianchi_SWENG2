require 'test_helper'

class DefaultLocationTest < ActiveSupport::TestCase
  # test that day of the week is between 0 and 6
	# test that a user doesn't have two default loactions starting at the same time
end
